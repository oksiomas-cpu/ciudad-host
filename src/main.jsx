import React from 'react'
import ReactDOM from 'react-dom/client'
import HostConsole from './HostConsole'
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode><HostConsole /></React.StrictMode>
)
